<?php
/**
 * Teachers archive (/teachers/) — reuses the directory page markup
 * so the archive never falls back to the blog layout.
 */
defined( 'ABSPATH' ) || exit;
require UTURN_DIR . '/page-teachers.php';
